name: datalocal_for_firestore
description: "Firestore synchronization and local materialized views for DataLocal."
repository: "https://github.com/lamun-my-id/datalocal_for_firestore.git"
version: 2.0.0-dev.1
# test    : dart pub publish --dry-run
# deploy  : dart pub publish
# flutter pub upgrade --major-versions

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  cloud_firestore: ^6.7.1
  datalocal: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  fake_cloud_firestore: ^4.2.0

flutter:
  uses-material-design: true

topics:
  - cache
  - database
  - firebase
  - firestore
  - offline

false_secrets:
  - /example/android/app/google-services.json
  - /example/ios/Runner/GoogleService-Info.plist
  - /example/lib/firebase_options.dart
  - /example/macos/Runner/GoogleService-Info.plist
